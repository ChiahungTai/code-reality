{
  "mcpServers": {
    "code-reality": {
      "type": "stdio",
      "command": "/bin/sh",
      "args": ["-c", "nb=\"${CLAUDE_PLUGIN_ROOT}/node_modules/.bin\"; if command -v code-reality-mcp >/dev/null 2>&1; then exec code-reality-mcp --stdio; fi; if [ -x \"$nb/code-reality-mcp\" ]; then PATH=\"$nb:$PATH\"; export PATH; exec \"$nb/code-reality-mcp\" --stdio; fi; for d in \"$HOME/.local/bin\" \"$HOME/.cargo/bin\"; do if [ -x \"$d/code-reality-mcp\" ]; then export PATH=\"$d:$PATH\"; exec \"$d/code-reality-mcp\" --stdio; fi; done; echo \"code-reality-mcp not found (PATH, plugin node_modules, ~/.local/bin, ~/.cargo/bin) — main face: uv tool install code-reality; embedded face: update the code-reality plugin\" >&2; exit 127"],
      "timeoutMs": 60000
    },
    "code-reality-lsp-bridge": {
      "type": "stdio",
      "command": "/bin/sh",
      "args": ["-c", "nb=\"${CLAUDE_PLUGIN_ROOT}/node_modules/.bin\"; if command -v code-reality-lsp-bridge >/dev/null 2>&1; then exec code-reality-lsp-bridge --stdio; fi; if [ -x \"$nb/code-reality-lsp-bridge\" ]; then PATH=\"$nb:$PATH\"; export PATH; exec \"$nb/code-reality-lsp-bridge\" --stdio; fi; for d in \"$HOME/.local/bin\" \"$HOME/.cargo/bin\"; do if [ -x \"$d/code-reality-lsp-bridge\" ]; then export PATH=\"$d:$PATH\"; exec \"$d/code-reality-lsp-bridge\" --stdio; fi; done; echo \"code-reality-lsp-bridge not found (PATH, plugin node_modules, ~/.local/bin, ~/.cargo/bin) — main face: uv tool install code-reality-lsp-bridge; embedded face: update the code-reality plugin\" >&2; exit 127"],
      "timeoutMs": 60000
    }
  }
}
